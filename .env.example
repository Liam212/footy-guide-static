VITE_API_URL=https://api.example.com
# Optional alternative to avoid browser CORS issues during local dev.
# If set, leave VITE_API_URL unset and the app will use /proxy via the Vite dev server.
VITE_API_PROXY_TARGET=https://api.example.com
